# Add patterns of files dvc should ignore, which could improve
# the performance. Learn more at
# https://dvc.org/doc/user-guide/dvcignore

# Never hash interpreter caches: .pyc files change without any source change
# and would make the analytics directory dependency (and thus the lock) drift.
__pycache__/
*.py[cod]
